# Tasks

- [ ] Add compute profiles
  - Add an option to create a compute profile, for example if need a cpu job, it will insert the defaults of a cpu job, if i want to run large llm, i need nodes1,2,3, but if i want to run small llm, i can use nodes4,5,6 as well, each scenario should save as a profile, which can be chosen when creating a new sbatch file (for example when creating a new project)
- [ ] Improve dialog
  - [ ] Show more inforamtion about each machine (like, what gpu it has, cpu, memory, etc.)
  - [ ] Add more guidance in the selection process
<!-- - [ ] Add auto symlinks -->
